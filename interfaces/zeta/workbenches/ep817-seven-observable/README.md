# Universal seven-observable image capacity

This is the standalone SplitZero receiving addition for the Erdős 817 seven-observable calculation. Its exact maps preserve the original support construction, word relations, finite-set observation kernels and specified metrics.

[SplitZero receiving maps](notes/splitzero-receiving-map.md). The complete mathematical note and standalone verification programs are included locally. No theta-source or arithmetic weight conclusion is inferred.

[Complete proof](notes/seven-observable-capacity.md) · [Producer](certificates/verify_seven_observable.py) · [Independent arithmetic auditor](certificates/audit_seven_observable.py) · [Claim inventory](polyclank/source_and_claims.json).

For every coefficient arity q, translated image counts close in 2^(q-2) coordinates. On the actual symmetric images, arity five has seven coordinates. A matrix of actual five-admissible concatenation counts has determinant 1,228,800, proving that seven are necessary for a universal linear tail-state realization. Every homogeneous image count obeys rho^m <= N_m <= C_D rho^m, with C_D <= q-1, independent of original rank and horizon. The global rate is the infimum of the corresponding seven-matrix spectral costs; the outer arithmetic family remains unbounded.

The full original numerical fibres have a bounded-size augmented simplex resolution, an explicit integral contraction, and a countable Hilbert extension in the stated unit face metrics. Original word masses retain their separate quotient formula. A quartic source and an interior Jordan chain test the actual spectral observation. The faithful reencoding comparison exactly constrains that specified compression class for the existing record sources.

## Reproduce

From this workbench directory:

```sh
python certificates/verify_seven_observable.py
python certificates/audit_seven_observable.py
```

Both programs use Python's standard library. The audit imports neither the producer nor its carrier library; it reconstructs the matrices by interpolation on actual finite joins. The normal and optimized Python runs have byte-identical respective receipts. The `--quick` option on the producer is intentionally a smaller scope and is not the recorded full replay.

The recorded domains contain 5,050 complete raw alphabets, 1,375 original two-level generator cases, 140 specified changing schedules, all 49 admissible Hankel entries, 2,828 numerical cut fibres, 126 simplex homotopy identities, 18 selected block/arity matrices and three faithful prime reencodings. The producer's 45,450 raw moment-vector comparisons and the independent selected 108 moment entries have separately stated scopes. Eight corrupted records are rejected by the independent audit.

## Evidence and attribution

These are ordinary mathematical proofs and finite computational certificates, submitted for independent mathematical review. No new Lean elaboration or external human validation is claimed. No complete general conjecture solution, all-rank stopping criterion, or new best numerical rate is asserted.

The original k=4 construction remains credited to the anonymous/deleted Reddit contributor. `sneed-and-feed` retains the separate Lean finite-upper-bound credit. Existing sources, receipts, and accepted-status files are unchanged.

The immediate predecessor archive is preserved unchanged in the cumulative delivery. Source revisions and theorem-local evidence are in `polyclank/`. Every recorded process has finished; no continuing background process is asserted.
