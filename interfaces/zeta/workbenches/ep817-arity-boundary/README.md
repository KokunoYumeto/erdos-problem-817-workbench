# Retained higher-arity kernels and real boundary observations

[Receiving-map note](notes/splitzero-receiving-map.md) · [Full arithmetic proof](notes/arity-and-boundary.md) · [Independent verification](certificates/independent_audit.json).

This additive EP817 return uses the existing SplitZero support reconstruction and internal quotient. It supplies exact additional evaluation kernels, weighted minimum sections, a visible nilpotent moment term, and a completion example whose finite terminal defect must remain a separate observation.

The complete standalone finite checkers and their receipt accompany this directory. They retain every original digit, residue, source mask, and metric mass. They do not import a theta-source estimate or assert a new arithmetic weight bound. No existing scalar, homology definition, dependency pin, Lean source, or accepted receipt is changed.

The inspected source pin and the exact reference roles are in the receiving note and `polyclank/claims.json`. Ordinary proofs and exact finite computations are submitted for independent review; no new Lean run or absent peer validation is claimed.

```sh
python certificates/verify_arity_boundary.py --output /tmp/arity_receipt.json
python certificates/audit_arity_boundary.py --input /tmp/arity_receipt.json \
  --output /tmp/arity_audit.json
```

The generic methods concern exact observed finite-state actions and their original kernels. In particular an unobserved largest eigenvalue can disappear, whereas an observed size-two primary block contributes a necessary `m*3**m` term. The real-limit examples retain a nonzero unscaled terminal defect at every finite support. Their numerical return cost is explicit.
