# Research state — 17 September 2026

## Baseline

Integrated EP817 main was read at `dbd0a93f2cf935103e88e5c2b2b71fe85ae7537b`.
The README and publication notes confirm integration of the corrected 29-note
collection and finite-period theorem. The current seven-observation separation
condition L>3Q and newer cone-dual scope are not replaced by older source copies.
This addition is independent of those presentation corrections and modifies no
inherited file.

## What changed mathematically

The previous work left an unbounded family of admissible arithmetic blocks.
The new residue theorem controls all heights and moduli at ranks one through
four. It uses the actual stabilizer of the ternary residue image. Multi-coset
cases force a forbidden small-order progression; one-coset cases retain an
explicit subgroup isomorphism and reduce to complete finite canonical domains.
The resulting prices 3,5,13,23 multiply on arbitrary numerical tails through an
actual residue-section injection. The remaining all-rank price is stated as a
rank-profile inequality rather than inferred from the low-rank result.

The composition theorem addresses the other remaining variation: frequency of
block types in an infinite schedule. An exact finite LP approximates every
frequency profile with uniform error. The radix-ten application is solved at
all finite compositions and all limiting frequencies. It gives a genuine
rational-frequency interval with no optimal periodic schedule; the explicitly
constructed phase-separated schedule attains the infimum there.

## Exact boundaries

Each rank-price block is individually modular-k-free. This is narrower than the
full carry-controlled class, whose singleton (2,4)-radix pattern can be safe
without each level being modular-safe. The note supplies the aggregation map
and retains the rank change. The profile inequality cannot be assigned to a
more general block presentation without checking this condition.

No unrestricted numerical rate improves here. The general lower estimate still
requires uniform information about the higher-rank arithmetic source. The
finite-dictionary frequency spectrum has a complete value and construction;
the entire all-rank infimum does not acquire a rank cutoff from that theorem.

## Evidence and next usable objects

The complete proofs are in notes/residue-and-composition.md. The 490 finite
obstruction blocks, all rank/profile examples, fixed-composition minima, and
84 exact composition LPs are in the plain JSON certificate. The separately
implemented auditor imports neither producer nor helper. Every recorded run
has finished; there is no background process represented by this handoff.

The reusable interfaces are the stabilizer reduction, the selected original
residue injection, the rank-dependent price inequality, the profile LP with
exact logarithmic dual prices, and the source-to-receiving kernel square.
The distribution of generators in higher-rank blocks is an actual remaining
arithmetic parameter. Subsequent work can strengthen that price without
repeating fixed-block word-length calculations.
