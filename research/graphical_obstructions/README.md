# Graphical obstruction propagation and a stronger six-term construction

This additive PolyClank contribution preserves actual positive generators, binary representation fibers, graph boundary maps, supported zeros, and original quotient metrics. Its ordinary proofs are submitted for independent mathematical review. No new Lean verification or priority claim is made.

## Results

The ten distances of `(0,4,7,41,257)`, namely `{3,4,7,34,37,41,216,250,253,257}`, give `lambda_6 <= 1651^(1/10)`, strictly below the previous workbench bound `93^(1/6)`. The actual subset-sum image has 291 values. The certificate includes every nonzero step modulo the composite base 1651, 81 returning carry columns, and all 548 smaller canonical-radix obstructions. It proves this block's exact optimum over every infinite canonical mixed-radix schedule, not the global optimum.

The complete-graph theorem is valid at every rank: a collision between two distinct tournament-score values forces an actual `(m+1)`-term progression. Its original integral correction is constructed using all multigraph score cuts, an integral allocation, and disjoint edge reversals. Consequently every admissible critical complete-graph projection is faithful on its score classes. Counting transitive scores, together with an explicit prime construction, gives the matching model law `m log(Xi_m)/log(m) -> 2`, even when rulers and radices change at every level. This is a theorem for the specified critical complete-graph source family, not a lower bound for all Erdős 817 blocks.

A failed extension is classified on the full parameter hyperplane `c=5b-3a`; varying the appended mark never repairs its actual six-term witness. Changing the prefix to `(0,4,7,41)` gives a completely classified finite exceptional range and an admissible infinite tail. The companion Bellman theorem constructs the greatest bounded monotone dual potential and distinguishes a repairable trial coefficient from a genuine arithmetic cycle obstruction.

## Proofs and retained data

`notes/graphical-obstructions.md` gives all graph, collision, asymptotic, extension, and record proofs. `notes/monotone-obstruction-dual.md` gives the canonical future-potential theorem and original reset-cost return. The unchanged two earlier proof providers are retained under `notes/predecessors/`.

`certificates/graphical_receipt.json` is the complete plain proof record. It contains the original binary masks, 3300 complete collision packets through five vertices, the complete lower-radix and exceptional-parameter coverage, original metrics, and eight Bellman calibrations using retained prior input files. The independent auditor imports no producer code and reconstructs the stated arithmetic domains. Larger-rank packet samples are labelled samples; the all-rank theorem is proved by the cut argument, not inferred from them.

From this contribution directory, run:

```sh
python certificates/verify_graphical_obstructions.py --output certificates/graphical_receipt.json
python certificates/audit_graphical_receipt.py --receipt certificates/graphical_receipt.json --output certificates/independent_audit.json
```

Both scripts require only the Python standard library. They use explicit runtime checks that remain active under `python -O`. The delivered normal and optimized producer receipts match byte-for-byte, as do the corresponding independent audit receipts. `polyclank/checks.json` records hashes and scopes. Commands run from an applied patch do not need the rest of either large workbench.

The prior eight compressed variable-block certificates are retained unchanged in `certificates/predecessor_inputs/`. This contribution checks the new future-potential identities on those exact inputs; it does not claim to rerun every old exhaustive profile audit.

## Source and publication boundary

The latest observed Erdős 817 main is `23c0110c95b5a2036bdc04a1f352b6e5e27742c8`; the observed outer-control branch is `74ba7dcc4bd623b3555046feb6d45ccac38417a3`. The Zeta reconstruction interface is pinned at `42df8a2de002d5fc7090641fac46ea11be05fa71`. The original anonymous/deleted Reddit mathematical credit and the separate `sneed-and-feed` Lean credit are retained.

The available GitHub connector was read-only in this continuation. No new remote commit, PR, or merge was created. The delivery provides an add-only patch and a prepared draft PR description. Existing accepted sources, status files, dependency pins, and Lean receipts are untouched.

The remaining global question is evaluation of the unrestricted all-rank certificate infimum. The complete-graph theorem shows exactly which proposed shortcut fails: collapsing critical score classes creates an AP. Improving the leading coefficient below two requires a genuinely broader source family or relation structure, with its original observation kernels and metric return retained.
