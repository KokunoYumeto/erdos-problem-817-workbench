# Local-pattern quotients and exact changing-block image capacity

Full mathematical proof: [local-flow-and-switching.md](notes/local-flow-and-switching.md).

This additive contribution identifies the complete closed cone of the seven fifth-arity observations: nine actual nonnegative pattern counts, two integral conservation identities, and ten primitive rays. It gives an explicit two-potential alternative producing either a positive decomposition or an actual finite symmetric counterexample. An all-arity graph-path substitution retains the zero-loop receiving kernel, the exact endpoint correction and the full original-current metric.

The actual radix-ten blocks {1,3} and {2,4} have an exact all-infinite-schedule image optimum 893^(1/6), attained by period AAB. Both homogeneous image rates are sqrt(10). This is a dictionary image-rate result, not a new unrestricted EP817 numerical record and not a change to the original radix cost. A separate theorem excludes faithful ternary re-encoding improvements for arbitrary mixtures of the earlier residue-saturated record blocks.

## Reproduce

From this directory:

```sh
python certificates/verify_local_flow.py
python certificates/audit_local_flow.py
```

Only Python's standard library is used. The auditor imports neither the producer nor its helper library. Detailed methods, domains, and corrupted-input controls are in the note and JSON receipts. Normal and optimized modes reproduce the same evidence bytes. No new Lean verification is claimed.

## Original sources and research status

The exact source revision, original contributor credits, claim-local proof locations and source hashes are in `polyclank/source_and_claims.json`. The Zeta support/quotient interface is cited at its inspected revision; no arithmetic theta estimate is imported. The preceding archive is preserved unchanged in the cumulative distribution. Main branches, inherited source files, and Lean receipts are not modified by this addition.

The remaining global task is a uniform arithmetic inequality on actual admissible H5 images, with generator count and source metric retained. The complete symmetric-set cone supplies valid necessary constraints, but its abstract extreme rays do not automatically have such arithmetic realizations. All recorded computations have finished; no ongoing process is claimed.
