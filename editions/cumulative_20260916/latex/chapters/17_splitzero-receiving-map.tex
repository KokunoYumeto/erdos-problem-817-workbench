% Original Markdown SHA-256: b530efe6075f73b8a9bf086cacbc78a02d24554a1d452fc75963a77752d99654
\chapter{Receiving progression closure and synchronized source witnesses}
\label{chapter:17_splitzero-receiving-map}
{\small\noindent\textbf{Source:} \nolinkurl{history/EP817_DEFECT_CLOSURE_20260915/payloads/zeta/workbenches/ep817-defect-closure/notes/splitzero-receiving-map.md}\
\textbf{Identity:} \nolinkurl{b530efe6075f73b8a9bf086cacbc78a02d24554a1d452fc75963a77752d99654}}
\medskip
15 September 2026. Ordinary mathematical contribution; independent mathematical review pending. No new Lean elaboration, theta-source estimate, or remote merge is claimed.

\section{Original interface}\label{n17-original-interface}

The directly inspected source is the original support reconstruction and internal quotient in \nolinkurl{workbenches/splitzero-tandem/tex/support_diagrams.tex}, equations (D6)-\/-(D8), at Zeta commit \nolinkurl{58626a62cd5648e8ae7450fb54d4a4aab2330981}. Its blob is \nolinkurl{d3493f891291ee6e94dbf2c77649f7d85d240df2}. In particular a relation at an admitted support maps to that support\textquotesingle s zero, and the additional kernel of a receiving observation is retained separately from the original boundary family.

The EP817 contribution applies this interface to two different, explicitly connected sources. The arithmetic-coordinate source is a rational vector space E containing a finite point set S. The word source is the free module on the actual binary masks, with the intermediate free module on their distinct score values. These modules are not identified merely because their points share numerical observations.

\section{1. A least receiving relation closure}\label{n17-a-least-receiving-relation-closure}

For k\textgreater=3 and an original rational relation subspace W\_0 subset E, define

\[
\begin{adjustbox}{max width=.92\linewidth}
$\displaystyle W_{j+1}=W_j+\operatorname{span}\{x_1-x_0:
  x_i\in S,\ x_i-2x_{i+1}+x_{i+2}\in W_j\text{ for every }i\}.$
\end{adjustbox}
\]

There are at most dim(E)-dim(W\_0) strict rank increases. The final W\_* has the exact universal description

\[
\begin{adjustbox}{max width=.92\linewidth}
$\displaystyle W_*=\bigcap_{\ell W_0=0,\ \ell(S)\text{ k-AP-free}}\ker\ell.$
\end{adjustbox}
\]

The intersection is over rational linear observations. Its proof is given in full in the companion EP817 note: necessity is induction through the recorded source tuples; sufficiency separates a specified surviving vector by a rational functional avoiding finitely many original pair-difference and second-difference hyperplanes. A bounded integer-polynomial search constructs that functional.

Every derived step therefore carries a source proof: either its scalar observation is zero, or its actual tuple is a nonconstant forbidden progression. A failed final constraint returns the first observed nonzero step, together with its numerical witness. Stabilization is an algebraic finite-rank statement, not an analytic compactness or weight estimate.

The relation extension remains the exact sequence

\[
\begin{adjustbox}{max width=.92\linewidth}
$\displaystyle 0\to W_*/W_0\to E/W_0\to E/W_*\to0.$
\end{adjustbox}
\]

W\_*/W\_0 is an additional receiving kernel forced on progression-free observations. It is not asserted to equal an original theta boundary. Integral lattices have the separate saturation arrow Z\^{}d/L -\textgreater{} Z\^{}d/(Q L intersect Z\^{}d), with its exact torsion kernel. Clearing the denominators in every displayed span identity retains its integral implication for torsion-free integer observations.

For S subset T, closure is monotone. More generally a specified linear f with f(S) subset T and f(W\_0) subset U\_0 satisfies \(f(W_*)\subset U_*\). This proves naturality of the quotient maps, so the original reconstructed SplitZero totals apply without introducing a replacement scalar.

\section{2. The word source, coordinate observation, and their different kernels}\label{n17-the-word-source-coordinate-observation-and-their-different-kernels}

For the graph application write

\[
\begin{adjustbox}{max width=.92\linewidth}
$\displaystyle M=\mathbb Q[\Omega],\quad V=\mathbb Q[\Sigma],\quad
\pi:M\twoheadrightarrow V,\quad p:V\to E,\quad p(e_s)=s.$
\end{adjustbox}
\]

The original orientation boundary is B=ker pi. For a closure stage W\_j define I\_j=p\^{}\{-1\}(W\_j) and K\_j=(p pi)\^{}\{-1\}(W\_j). Then

\[
\begin{adjustbox}{max width=.92\linewidth}
$\displaystyle 0\to B\to K_j\xrightarrow\pi I_j\to0.$
\end{adjustbox}
\]

These arrows explicitly locate the coordinate-observation kernel in the original word presentation. The original fibre relation is not silently replaced by all scalar or coordinate cancellations. All maps commute with the admitted support inclusions where the corresponding source and relation families are included.

For an actual numeric value map J:V-\textgreater Q{[}H(A){]}, J(e\_s)=e\_\{C\_T-L\_T(s)\}, the new K\_4/K\_\{3,3\} theorem proves J injective under five-admissibility and distinct positive edge generators. Consequently ker(J pi)=B at that \emph{value-basis} level. The later amplitude observation Q{[}H(A){]}-\textgreater Q, e\_y-\textgreater y, still has its separately retained kernel. The equality of word/value relation kernels does not assert injectivity of amplitude on a free module.

\section{3. Why binary-switch data survive an unbounded tensor source}\label{n17-why-binary-switch-data-survive-an-unbounded-tensor-source}

A local certificate keeps an actual source tuple

\[
\begin{adjustbox}{max width=.92\linewidth}
$\displaystyle x_i=a+i v+\sigma_i r\in S,\qquad\sigma_i\in\{0,1\}.$
\end{adjustbox}
\]

Its consecutive defects are (Delta sigma\_i)r. If a collision in a product source has full defect r=(r\_1,...,r\_h,r\_Y), place this packet in one factor j and choose x\_l+sigma\_i r\_l in every other factor. Those are exactly one of the two original source points, so no convexification or linear-span substitution occurs. Total evaluation is C+i ell\_j(v)+sigma\_i ell(r), hence a progression whenever ell(r)=0.

The original last-defect form uses sigma=00001. The new K\_\{3,3\} proof requires seven additional orbit types using 00011, usually with two step vectors whose difference is a vertex root. The one exceptional doubled defect records its factor two and a separate local packet. The scalar alternatives are proved before any tensor step; the full collision is never divided componentwise.

This produces an all-rank core-isolation theorem: an arbitrary number of disjoint K\_4 or K\_\{3,3\} distance blocks have no new binary-value collision with each other or with an arbitrary remaining generator set in a five-admissible A. The resulting addition bijection and original multiplicity formula are

\[
\begin{adjustbox}{max width=.92\linewidth}
$\displaystyle \prod_j H(A_j)\times H(B)\overset\sim\longrightarrow H(A),\qquad
\mu_A(z+\sum y_j)=\mu_B(z)\prod_j\mu_{A_j}(y_j).$
\end{adjustbox}
\]

This theorem makes no scale-separation or radix-schedule assumption. The unrestricted number of factors is handled by the displayed synchronized formula, not by extrapolation from a finite product enumeration.

\section{4. Original metrics and an arithmetic consequence}\label{n17-original-metrics-and-an-arithmetic-consequence}

The word quotient metric is still diag(1/mu). For a K\_\{3,3\} core the score multiplicities are 230 ones, 54 twos, 24 threes, 18 fives and two sixes. The uniform measure on 328 distinct scores is therefore different from the measure on 512 uniformly weighted masks. The exact unweighted covariance is

\[
\begin{adjustbox}{max width=.92\linewidth}
$\displaystyle \frac{25}{82}\mathcal L_{K_{3,3}}-\frac1{164}uu^T,
\quad u=(1,1,1,-1,-1,-1).$
\end{adjustbox}
\]

For a K\_4 core it is (25/76) times its graph Laplacian. All cross terms are computed in the original vertex coordinates. The core-isolation bijection makes unweighted variances additive, including the actual reservoir variance.

For h K\_\{3,3\} cores and no remaining generators, Q=sum a\^{}2 and the original part-sum imbalances D\_j obey

\[
\begin{adjustbox}{max width=.92\linewidth}
$\displaystyle 41(328^{2h}-1)\le150Q-3\sum_jD_j^2.$
\end{adjustbox}
\]

The quotient-to-unit-value metric return has norm 6\^{}(h/2). For a mixed core collection it is \(4^{a/2}6^{b/2}\sqrt{\max\mu_B}\). These costs remain present despite the exact value-space bijection.

\section{5. Scope and evidence}\label{n17-scope-and-evidence}

The full companion note, four standard-library verification files, compressed certificate and receipts accompany this receiving note. The certificate covers all 512 K\_\{3,3\} head masks, all 5,298 nonzero score differences in 99 explicitly transported orbits, and all 200 K\_4 differences. A separate auditor imports neither producer nor closure library. It also checks all 592 stated small closure instances, 140 positive-distinct models, 452 exact nonexistence alternatives and 35 multi-core integer witness cases. The direct reservoir scan covers 5,285 singleton extensions and two specified larger reservoirs. Eight corrupted proof records are rejected.

No new file is asserted to be Lean-checked. The source identities and normal/optimized execution outcomes belong to the integration record. These finite verifications certify the local data and stated replay domains; the arbitrary-rank conclusions use the explicit proofs.

The global EP817 rate remains unevaluated. Not every admissible generator family has a K\_4/K\_\{3,3\} core packing, and the general closure fixed points have not been classified at unbounded generator rank. Those are separate arithmetic obligations, not consequences of a finite-dimensional auxiliary representation.

\section{Source attribution}\label{n17-source-attribution}

The original SplitZero source retains its existing attribution and formalization scope. The original EP817 k=4 construction belongs to the anonymous/deleted Reddit contributor, with sneed-and-feed separately credited for the Lean upper extension. The local K\_4 data here are independently replayed; the K\_\{3,3\} two-switch certificates and their reservoir synchronization are the present workbench contribution. No identity inference, private correspondence or copied private transcript is included.
