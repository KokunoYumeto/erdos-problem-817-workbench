% Original Markdown SHA-256: b035e783df8fb9aaffea60089e302ee8da705e2ce882ed1b2f045c54d4fbd7a3
\chapter{Canonical factor supports and integral progression-observation kernels}
\label{chapter:19_splitzero-receiving-map}
{\small\noindent\textbf{Source:} \nolinkurl{history/EP817_CANONICAL_ISOLATION_20260916/payloads/zeta/workbenches/ep817-canonical-isolation/notes/splitzero-receiving-map.md}\
\textbf{Identity:} \nolinkurl{b035e783df8fb9aaffea60089e302ee8da705e2ce882ed1b2f045c54d4fbd7a3}}
\medskip
The Clankers, 16 September 2026. Additive receiving contribution from the Erdős Problem 817 workbench. All new claims have written proofs and finite exact checks with their stated domains. No new Lean elaboration or analytic arithmetic-weight conclusion is claimed.

\section{1. Original interface and the new mathematical inputs}\label{n19-original-interface-and-the-new-mathematical-inputs}

The receiving interface is the original diagram reconstruction and internal quotient in \nolinkurl{workbenches/splitzero-tandem/tex/support_diagrams.tex}, equations D6--D8, exact blob \nolinkurl{d3493f891291ee6e94dbf2c77649f7d85d240df2}, inspected at commit \nolinkurl{58626a62cd5648e8ae7450fb54d4a4aab2330981}. Its two kernel constructions distinguish reaching the supported zero in a receiving fibre from reaching global absence. The present construction instantiates those diagrams; it introduces no replacement scalar or homology convention.

The companion \texttt{canonical-isolation.md} gives the complete arithmetic proofs. Its main new input is that every complete-graph orientation-score source Sigma\_m is universally (m+1)-isolating under arbitrary rational observations and finite additive remainders. The forced relation r is derived in the unchanged source Sigma\_m union (Sigma\_m+r), in at most floor(m\^{}2/4) integral root steps. The proof retains each original tuple and its actual preceding-boundary coefficients.

A second input is the canonical factor partition of a positive integer digit image. Its components are obtained from the supports of the original row\textquotesingle s conformal-minimal integer relations (classical Graver elements) as they enter the literal coefficient box. This is a partition of admitted generator coordinates, not a division of the coordinate weights or a reduction to a different source.

The finite triangular-prism and K3,3 calculations identify three distinct implication mechanisms: an original integral consequence, an original torsion class killed by a torsion-free receiving scalar, and a free first-round class killed only after an additional actual progression consequence. Those cases must not be merged into a single unspecified zero object.

\section{2. Fixed-source implication diagrams}\label{n19-fixed-source-implication-diagrams}

Let U be a finite subset of a rational vector space E, with every u accompanied by its original representation fibre. For a specified sequence of derived relation spaces W\_j, retain

\[
\begin{adjustbox}{max width=.92\linewidth}
$\displaystyle 0\longrightarrow W_{j+1}/W_j\longrightarrow E/W_j
\longrightarrow E/W_{j+1}\longrightarrow0.$
\end{adjustbox}
\tag{2.1}
\]

The relations newly admitted at stage j+1 are steps v=x\_1-x\_0 of actual tuples x in U\^{}k whose second differences lie in W\_j. The certificate records explicit expressions for each defect in the original W\_j basis. A k-AP-free receiving observation annihilating W\_j annihilates v, because it maps that actual tuple to a progression. Equation (2.1) identifies the additional receiving kernel rather than rewriting W\_0.

For the clique, start with r in Sigma\_m-Sigma\_m, choose at each stage i maximal and j minimal in the current r\_t, and put alpha\_t=e\_j-e\_i. The packet construction gives rows x\_l in Sigma\_m. After adding the ORIGINAL r to the first m rows, all rows remain in U=Sigma\_m union (Sigma\_m+r). The only possible nonzero second difference is

\[
\begin{adjustbox}{max width=.92\linewidth}
$\displaystyle r_t-r=\sum_{h<t}\alpha_h,$
\end{adjustbox}
\]

while its step is alpha\_t. The positive mass of r\_t decreases by one, and the final identity is

\[
\begin{adjustbox}{max width=.92\linewidth}
$\displaystyle r=-\sum_t\alpha_t.$
\end{adjustbox}
\tag{2.2}
\]

Thus every relation in this derivation has an integral primitive in the preceding relation list. A numerical map detecting r has a first derived alpha\_t with nonzero image. Its actual source tuple supplies a nonconstant numerical progression. This gives an explicit obstruction map from a failed quotient implication to original arithmetic witnesses.

No arbitrary section of E/W\_j is asserted natural. The certificates are actual representatives in U; they require no replacement of a defect by a source point that exists only in a linear span.

\section{3. A retained order-two class and a separate free class}\label{n19-a-retained-order-two-class-and-a-separate-free-class}

For three K3,3 two-translate sources, let L\_1 be the integer span of ALL first-round vector-progression steps. The proof data give

\[
\begin{adjustbox}{max width=.92\linewidth}
$\displaystyle 2r\in L_1,\qquad r\notin L_1.$
\end{adjustbox}
\tag{3.1}
\]

The exclusion is certified by eta=(1,1,1,0,0,0), whose pairing with every first-round generator is even and whose pairing with r is odd. All first-round directions are independently enumerated from the original source, so the parity certificate is not an artifact of omitting a possible generator.

The map

\[
\begin{adjustbox}{max width=.92\linewidth}
$\displaystyle \mathbb Z^6/L_1\longrightarrow
\mathbb Q^6/\mathbb QL_1$
\end{adjustbox}
\tag{3.2}
\]

has kernel (sat L\_1)/L\_1. The class of r has exact order two in that kernel. An integer or rational numerical observation killing L\_1 kills r because its target has no torsion. This is a statement about that receiving scalar, not equality of r and zero in the original integer quotient.

For two other K3,3 classes, an integer coordinate functional vanishes on all first-round steps but detects r. Those classes survive rationalization. The next certificate supplies an actual tuple in the SAME two-translate source, whose three defects are specified integer combinations of the first-round steps. Its step w is newly forced to vanish, and r is then an explicit integral combination of the old steps and w.

The exact diagrams differ: (3.2) describes a scalar-extension kernel, while the second case uses

\[
\begin{adjustbox}{max width=.92\linewidth}
$\displaystyle L_2/L_1\longrightarrow\mathbb Z^6/L_1\longrightarrow\mathbb Z^6/L_2.$
\end{adjustbox}
\tag{3.3}
\]

Keeping both diagrams makes the distinction testable. The negative controls erase the multiplier in (3.1) or alter the old-boundary coefficients in (3.3), and both alterations are rejected.

\section{4. Canonical generator partitions and their source maps}\label{n19-canonical-generator-partitions-and-their-source-maps}

For positive distinct A=(a\_i), let Q\_q=\{0,...,q-1\}\^{}n and Phi\_A(x)=sum a\_ix\_i. For each q, the canonical partition Pi\_q is generated by supports of the original Graver elements g with norm\_infinity(g)\textless=q-1. A conformal decomposition of any bounded relation stays in that same coefficient box.

The original word map is a bijection

\[
\begin{adjustbox}{max width=.92\linewidth}
$\displaystyle Q_q\overset\sim\longrightarrow
\prod_{C\in\Pi_q}\{0,\ldots,q-1\}^{C},\qquad
x\longmapsto(x|_C)_C.$
\end{adjustbox}
\tag{4.1}
\]

The induced value map

\[
\begin{adjustbox}{max width=.92\linewidth}
$\displaystyle \prod_C H_q(A_C)\overset{+}{\longrightarrow}H_q(A)$
\end{adjustbox}
\tag{4.2}
\]

is also a bijection. Its inverse is well-defined by the componentwise equality theorem: two words with the same full value have the same value on every component.

Let F\_C be the free rational word module for a component and R\_C its value-representation kernel. The source tensor product is the original free module on Q\_q. Its local relation module is

\[
\begin{adjustbox}{max width=.92\linewidth}
$\displaystyle R_{\rm loc}=
\sum_C\left(\bigotimes_{D<C}F_D\right)\otimes R_C\otimes
\left(\bigotimes_{D>C}F_D\right).$
\end{adjustbox}
\tag{4.3}
\]

It is the full global value-representation kernel. To see this directly, any two words in one full numerical fibre have equal component values. Change their component words one component at a time; their difference telescopes into the displayed local relation summands. This gives an actual primitive for every global same-value relation.

The resulting quotient is

\[
\begin{adjustbox}{max width=.92\linewidth}
$\displaystyle \bigotimes_C(F_C/R_C)\overset\sim\longrightarrow
\mathbb Q[H_q(A)],$
\end{adjustbox}
\tag{4.4}
\]

with the same product-value basis as (4.2). These maps commute with inclusion of admitted word supports wherever the component product support is specified. At arbitrary supports, retain the actual image subspaces and restrict the same evaluation square rather than assuming every support is a Cartesian product.

A zero value y=0 has a present basis vector e\_0 in the free value space. Sending e\_y to the numerical scalar y is a further observation with its own kernel. It is not the value-representation quotient itself.

\section{5. Arity growth, partitions, and the extra receiving kernel}\label{n19-arity-growth-partitions-and-the-extra-receiving-kernel}

For q\textless=q\textquotesingle, the literal inclusion Q\_q-\textgreater Q\_(q\textquotesingle) and inclusion of numerical images commute with Phi\_A. The partition Pi\_q refines Pi\_(q\textquotesingle), since every earlier primitive support remains admitted. The component map sends an old part to the larger receiving part containing it.

A complete finite description of the one-row primitive family is available at the original scale. Every primitive g has l1 mass at most 2*max(A)-1; the proof keeps its signed multiset, an alternating-sign order, and every distinct nonzero partial sum. Pairwise primitives show that all components have merged by q=max(A)+1. These statements retain the actual dependence on max(A); they do not make the filtration uniform as the arithmetic height varies.

The original binary factorization for

\[
\begin{adjustbox}{max width=.92\linewidth}
$\displaystyle A=(1,4,5,17,21,22,97)$
\end{adjustbox}
\]

has parts C=(1,4,5,17,21,22) and \{97\}. At q=3, retain the OLD factor-value source

\[
\begin{adjustbox}{max width=.92\linewidth}
$\displaystyle F=\mathbb Q[H_3(C)\times\{0,97,194\}]$
\end{adjustbox}
\]

rather than immediately relabelling it as the receiving image. Its dimension is 417. The sum map J:F-\textgreater Q{[}H\_3(A){]} is onto a 333-dimensional space, with kernel rank 84. The explicit relation

\[
\begin{adjustbox}{max width=.92\linewidth}
$\displaystyle 97=1+2\cdot5+2\cdot21+2\cdot22$
\end{adjustbox}
\]

is one actual ternary witness.

On the original ternary word source V, with q\_loc:V-\textgreater F, the exact receiving comparison is

\[
\begin{adjustbox}{max width=.92\linewidth}
$\displaystyle 0\to\ker q_{\rm loc}\to\ker(Jq_{\rm loc})
\xrightarrow{q_{\rm loc}}\ker J\to0.$
\end{adjustbox}
\tag{5.1}
\]

Thus the 84 receiving directions are visible before quotienting them. The same example has binary image size 76=38*2 and is five-AP-free. It establishes precisely why a valid binary isolation certificate does not license erasing a later additive observation kernel.

\section{6. Original metrics and canonical core support}\label{n19-original-metrics-and-canonical-core-support}

Give each original word basis vector norm one. If mu(y) is the number of words with value y, the least-norm representative of e\_y is the average of all mu(y) original words. Therefore the original quotient Gram is diag(1/mu(y)). Under (4.1)--(4.4), multiplicities multiply and these Grams tensorize. The norm of the identity to the unit value metric is sqrt(max mu), with every factor retained.

The prism\textquotesingle s unweighted distinct-score covariance is

\[
\begin{adjustbox}{max width=.92\linewidth}
$\displaystyle \frac{49}{157}L_P-\frac{13}{314}P_{\rm part}
-\frac8{157}P_{\rm anti,0}.$
\end{adjustbox}
\]

The full original-coordinate matrix, nonedge terms and multiplicity histogram are retained in the certificate. Uniform original words have a different pushforward measure; their covariance is L\_P/4. The quotient map and its multiplicity fibres give the exact relation between those two measures.

Finally, a certified complete-graph core in the critical progression length, or a K3,3/prism core at k=5, is an entire canonical binary component. The short signed cycle relations are conformally minimal and connect its edges, while universal isolation excludes cross-component binary collisions. Two cores are consequently disjoint or identical. This replaces a chosen packing by a canonical subset of the original generator-support partition.

The theorem does not identify every remainder component with a graph core. Nor does it bound the extra q-ary kernel in (5.1) across arbitrary ranks. Those are arithmetic tasks still outside the proved scope.

\section{7. Evidence and integration boundary}\label{n19-evidence-and-integration-boundary}

The accompanying arithmetic note and standalone verifiers are copied byte-for-byte into this receiving payload. They need no files from the EP817 repository and perform no network operation. \texttt{audit\_certificate.py} imports neither the producer nor its source library. It reconstructs original orientation fibres, exact symmetry coverage, every closure equation, exceptional first-round direction sets, and canonical factor partitions by a separate method.

The all-rank clique result has a written integral proof. Its complete finite replay is through five vertices, with larger source-mask examples explicitly labelled as samples. The prism and K3,3 proofs cover their entire stated finite difference domains. Arity and primitive-domain replays retain their exact finite bounds. The integration receipt, not this source narrative, records which final normal/optimized and exported-patch runs were observed.

The original G(R), the original supported quotient convention and the existing analytic cohomology are unchanged. No theorem here asserts that the theta observation has a finite exhaustive kernel, or that the Zeta programme\textquotesingle s growing arithmetic metric is controlled by the finite graph examples. The transferred techniques are the integral fixed-source implication chain, its retained torsion/free receiving distinctions, the canonical component filtration, and their actual tensor/source metric formulas.
